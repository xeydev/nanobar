import SwiftUI
@_exported import NanoBarPluginAPI

// nanoPill(), GlassPillModifier, stableMinWidth, Theme, InteractiveRegion, and monitorID
// all live in NanoBarPluginAPI so plugins can use them directly.
